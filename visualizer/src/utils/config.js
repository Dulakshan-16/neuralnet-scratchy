// Spacings are given in pixels

// Horizontal spacing between two layers
export const layerSpacing = 100;

export const nodeRadius = 50;
// Vertical spacing between two nodes
export const nodeSpacing = 50;

// The width of a layer
export const layerWidth = nodeRadius * 2 + layerSpacing;
